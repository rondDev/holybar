import { Button, VerticalBox } from "std-widgets.slint";


export component MainWindow inherits Window {
    in-out property<string> time: "12:34:56";
    callback timechange();
    width: 3440px;
    height: 40px;
    title: "holybar";
    HorizontalLayout {
        Text {
            text: "\{root.time}";
        }
    }
}
